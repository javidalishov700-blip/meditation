OPENAI_API_KEY=
# Optional: live TTS host for languages without a baked MP3 (de/it, or before bake)
VITE_TTS_URL=

# Never commit the key. After you paste it here, run:
#   npm run bake-voice
# Store builds (Codemagic) bake when OPENAI_API_KEY is set as a secret, then pack the MP3s into the IPA.
# Voice langs: tr az en ru es fr  (de/it stay live TTS)
