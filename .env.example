OPENAI_API_KEY=
# One-time bake only. Never in the IPA. Never in git.
# Better meditation voice (coral): put credit on this OpenAI key, then:
#   npm run bake-voice:med
# If the key has no credit, Edge neural still works:
#   npm run bake-voice:med-edge
# Listening never calls OpenAI. SOS + meditation MP3s cache on first open (PWA)
# and ship inside the Capacitor app (offline).

# Optional CDN / own hosting. Empty = same-origin /voice/ (PWA + Capacitor bundle).
# Example: https://files.your-domain.com/voice
VITE_VOICE_URL=

# Empty = no remote crash reports. Optional Sentry DSN (privacy: off by default).
VITE_SENTRY_DSN=

# iOS public SDK key (appl_…). Empty = shop stays a device demo. Never the secret key.
# Codemagic → Environment variables (encrypted). Product ids: store/app-store-products.json
VITE_REVENUECAT_IOS_KEY=

# Public origin for /legal/privacy.html and /legal/terms.html (App Store Connect).
# Empty = same-origin paths (PWA and the Capacitor bundle).
# App Store Connect cannot open a private GitHub repo — host these two HTML files on any public https origin.
VITE_LEGAL_ORIGIN=

# Optional. Codemagic Apple integration usually sets the team; use only if signing asks for it.
APPLE_TEAM_ID=

# Codemagic encrypted group `1` (matches the UI group name). Never commit.
#   openssl genrsa 2048
# Paste the PEM into CERTIFICATE_PRIVATE_KEY. Keep the same key forever.
# CERTIFICATE_PRIVATE_KEY=
